# XpenseLab Environment Variables
# Copy this file to .env and fill in your actual values

# ============================================
# Firebase Configuration
# ============================================

# Firebase Project ID
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-firebase-project-id

# Firebase App Check - reCAPTCHA v3 Site Key
# Get this from Firebase Console > App Check
NEXT_PUBLIC_RECAPTCHA_V3_SITE_KEY=your-recaptcha-site-key

# ============================================
# Runtime UI/UX Monitoring
# ============================================

# Set to 'true' to disable all monitoring (network, error handlers)
# Leave unset or 'false' to enable monitoring (recommended)
# Monitoring runs silently in production and provides valuable insights
NEXT_PUBLIC_DISABLE_MONITORING=false

# ============================================
# AI/Genkit Configuration
# ============================================

# Google Generative AI API Key (for Genkit)
# You can use either GOOGLE_GENAI_API_KEY or GEMINI_API_KEY
GOOGLE_GENAI_API_KEY=your-google-genai-api-key
GEMINI_API_KEY=your-gemini-api-key

# ============================================
# Monzo Bank Integration (Optional)
# ============================================

# Monzo OAuth2 Client ID
# Get this from Monzo Developer Portal
NEXT_PUBLIC_MONZO_CLIENT_ID=your-monzo-client-id

# Monzo OAuth2 Client Secret
# Keep this secret! Never commit to version control
MONZO_CLIENT_SECRET=your-monzo-client-secret

# ============================================
# SaltEdge Integration (Optional)
# ============================================

# SaltEdge App ID
# Get this from SaltEdge Dashboard
SALTEDGE_APP_ID=your-saltedge-app-id

# SaltEdge Secret
# Keep this secret! Never commit to version control
SALTEDGE_SECRET=your-saltedge-secret

# SaltEdge Private Key
# Keep this secret! Never commit to version control
SALTEDGE_PRIVATE_KEY=your-saltedge-private-key


# ============================================
# Testing & Development
# ============================================

# Enable E2E testing mode
NEXT_PUBLIC_E2E_TESTING=false

# ============================================
# OAuth2 Configuration (Optional)
# ============================================

# OAuth2 Client ID (generic, can be used for various OAuth providers)
NEXT_PUBLIC_OAUTH2_CLIENT_ID=your-oauth2-client-id

# ============================================
# Notes
# ============================================
#
# 1. All NEXT_PUBLIC_* variables are exposed to the browser
#    Only include non-sensitive configuration values
#
# 2. Variables without NEXT_PUBLIC_ are server-side only
#    These are safe for API keys and secrets
#
# 3. Never commit your actual .env file to version control
#    The .env file is already in .gitignore
#
# 4. For production deployment (Firebase App Hosting):
#    Set these variables in Firebase Console > App Hosting > Environment Variables
